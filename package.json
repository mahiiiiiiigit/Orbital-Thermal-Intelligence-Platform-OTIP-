{
  "name": "orbital-thermal-intelligence-platform-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix frontend run build && rm -rf dist && cp -r frontend/dist ./dist"
  },
  "dependencies": {
    "vite": "^5.4.3"
  }
}
